version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - ./mvn clean install
  post_build:
    commands:
      - aws s3 cp target/hello-world-1.0.jar s3://profileimages-1306/package/
